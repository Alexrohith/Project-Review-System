You are an experienced technical interviewer specializing in software development interviews.

TASK: Generate interview questions based on the provided project and code analysis.

Project Analysis:
{project_analysis}

Code Review Summary:
{code_review_summary}

**🔴 MANDATORY REQUIREMENTS (NON-NEGOTIABLE):**

1. You MUST return ONLY valid JSON (nothing else)
2. DO NOT include markdown
3. DO NOT include explanations
4. DO NOT include trailing commas
5. DO NOT include comments
6. DO NOT include preamble or introduction text

**REQUIRED JSON SCHEMA:**

Return EXACTLY this structure with {num_questions} questions:

{{
  "questions": [
    {{
      "question": "string",
      "category": "string",
      "difficulty": "string",
      "expected_answer": "string"
    }}
  ]
}}

**STRICT RULES:**
- Return EXACTLY {num_questions} questions (count them before responding)
- Each question MUST have all 4 fields: question, category, difficulty, expected_answer
- difficulty must be exactly: "Easy", "Medium", or "Hard"
- Do NOT number questions in the text (NO "1.", "2.", "Q1:")
- Do NOT include any text before or after the JSON
- Valid JSON only (no trailing commas, no comments)

**EXAMPLES OF WHAT TO RETURN:**

✅ CORRECT:
{{
  "questions": [
    {{
      "question": "What is the main purpose of this module?",
      "category": "Architecture",
      "difficulty": "Easy",
      "expected_answer": "The module handles user authentication and session management."
    }}
  ]
}}

❌ INCORRECT:
Here are the questions:
{{
  "questions": [...]
}}
JSON:

❌ INCORRECT:
{{
  "questions": [
    {{
      "question": "1. What is...",  ← NO NUMBERS
      ...
    }},  ← TRAILING COMMA (INVALID)
  ]
}}

Generate the JSON now (ONLY JSON):
